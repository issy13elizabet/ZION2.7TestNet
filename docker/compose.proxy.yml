services:
  zion-proxy:
    image: nginx:alpine
    container_name: zion-proxy
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - zion-seeds

networks:
  zion-seeds:
    external: true
